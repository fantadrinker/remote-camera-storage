/**
 * This module handles incoming SNS notification and routes the message to the 
 * appropriate websocket connection, all info should be contained in the notification
 * record
 * */

export const routeMessage = (event, context) => {
  console.log("hello")
  return;
}
